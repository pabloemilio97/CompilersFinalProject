program programa;
let int a;
let int fib;
let int memo[101];

int function fibonacci(int x)
{
    if(memo[x] == 0 - 1){
        memo[x] = fibonacci(x-1) + fibonacci(x-2);
    }
    return memo[x];
}

main() {
    fib = 100;
    for (a = 0 to fib + 1) {
        memo[a] = 0 - 1;
    }
    memo[0] = 0;
    memo[1] = 1;
    a = fibonacci(fib);
    for (a = 0 to fib) {
        write(memo[a]);
    }
}
