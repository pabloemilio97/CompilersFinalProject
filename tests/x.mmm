program test;
let int arr[10], a;

int function multiplyNums(int x, int y)
{
    return x * y;
}

int function sumNums(int x, int y)
{
    return x + y;
}

main() {
    for (a = 0 to 10) {
        arr[a] = sumNums(multiplyNums(a, a), multiplyNums(a, a));
        write(arr[a]);
    }
}