program test;
let int arr[10], x;

int function multiplyNums(int x, int y)
{
    return x * y;
}

int function sumNums(int x, int y)
{
    return x + y;
}

main() {
    for (x = 0 to 10) {
        arr[x] = sumNums(multiplyNums(x, x), multiplyNums(x, x));
        write(arr[x]);
    }
}